# 現状サマリー（development_status）

別チャット・将来のAI（ChatGPT・Claude Code問わず）が、このプロジェクトの「今」を素早く把握するための1ファイル。詳細な経緯は各docsを参照。このファイルは**要点のみ**を保ち、詳細を書きたくなったら該当するdocsへ書いて、ここからはリンクする。

**最終更新日: 2026-07-28**

## 現在のフェーズ

- ロードマップ（[02_roadmap.md](./02_roadmap.md)）上は **Phase 4（Python分析API）の途中**。
- 加えて、Phase 4.5として**依頼者確認用のWeb公開**まで完了している（[05_tasks.md](./05_tasks.md)参照）。
- Phase 3（Common Crawl連携、DataForSEO Live APIの常時運用化）、Phase 5（PostgreSQL永続化）、Phase 6（プロダクション化）は未着手。DataForSEO **Sandbox**への基本的な接続、および**5つの手動確認用ゲートが揃った場合に限るLive本番API接続**はPhase 4側（AI Overview比較）で先行実装済み。
- 解析エンジンの内部設計を整理した[11_architecture_v1.md](./11_architecture_v1.md)（v1.0アーキテクチャ設計書）を追加済み（2026-07-15）。Common Crawl / DataForSEOなど取得元が増えても解析側を変えずに済むよう、すべての取得元を`Document[]`へ変換する「Document Pipeline」の考え方を明文化した。
- Document Pipeline（Provider→Cleaner→Normalizer→Chunker→Analyzer）の5段階すべてが実装済みになった（Chunkerを2026-07-16に追加）。`Document`型定義・Providerレベルの変換（`user_provided`/`web_fetch`/`development_sample`→`Document[]`）・Cleanerの分離（`backend/services/document_cleaner.py`）・Normalizerの追加（`backend/services/document_normalizer.py`）・Chunkerの追加（`backend/services/document_chunker.py`）・Analyzer側アダプターまで実装済み。同日、軽量な文脈分析（`backend/services/context_analysis.py`）がChunkerの出力を実際に消費する最初のAnalyzerロジックになった。共起解析は引き続き`Document.text`全体を直接読み、Chunker非経由のまま。さらに同日、軽量なブランド認知サマリー（`backend/services/brand_summary.py`）が共起解析・文脈分析の結果から`summary`を実データ由来にし、軽量な改善提案（`backend/services/improvement_suggestions.py`）が共起解析・文脈分析・ブランド認知サマリーの結果から`improvements`を実データ由来にした。2026-07-17には、残る唯一のmockセクションであるAI Overview比較にもprovider切り替え基盤（`backend/services/ai_overview_provider.py`、mock/off/dataforseoモード）が導入され、同日中にDataForSEO認証情報・実行安全ルールの設計（`backend/services/dataforseo_settings.py`）、さらにDataForSEO **Sandbox**への実際のHTTP接続（`backend/services/dataforseo_client.py`）まで実装された。2026-07-23には、Vercel→Render→DataForSEO Sandboxの手動確認を経て、`dataforseo`モードの標準SERPエンドポイントを`google_organic_live_advanced`から`google_ai_mode_live_advanced`へ切り替え、`location_code`/`language_code`/`device`/`os`を環境変数化した。同日中に、さらに**DataForSEO Live本番APIへの接続**を、5つの明示的な環境変数ゲート（`DATAFORSEO_API_ENV=live`・`DATAFORSEO_LIVE_API_ENABLED=true`・`DATAFORSEO_LIVE_CONFIRM_TEXT=ALLOW_DATAFORSEO_LIVE_ONCE`との完全一致・`DATAFORSEO_REQUEST_LIMIT_PER_ANALYZE=1`・認証情報設定済み）がすべて揃った場合のみ許可する「手動での1回限りの確認」経路として実装した（詳細は[07_decisions.md](./07_decisions.md)）。通常運用のデフォルトは引き続き`mock`のまま（詳細は下記「実装済み機能」、[11_architecture_v1.md](./11_architecture_v1.md)の「10. 次フェーズ候補」）。同じく2026-07-23、依頼者への提出に向けた暫定対応として、OpenAI APIを使ったChatGPT相当モデルの1問観測（`backend/services/chatgpt_settings.py`/`chatgpt_client.py`/`chatgpt_provider.py`）をAI Overview比較へ追加した。DataForSEOと同じ2段階ゲート（`CHATGPT_PROVIDER_MODE`デフォルト`off`＋`ALLOW_CHATGPT_MODE_OVERRIDE`デフォルト`false`）で保護されたデフォルトoffの検証用機能であり、常時運用ではない（詳細は下記「実装済み機能」、[07_decisions.md](./07_decisions.md)）。

## 実装済み機能

- ブランド名入力 → 分析開始 → 5セクション結果表示（サマリー / 共起語ランキング / 文脈分析 / AI Overview比較 / 改善提案）の一連のUI（`app/page.tsx`、`app/components/`）
- 分析対象URLの入力UI（複数行テキストエリア、1行1件・最大10件・空行除外・重複除外・http(s)形式チェック。[BrandInputForm.tsx](../app/components/BrandInputForm.tsx)、[url-validation.ts](../app/lib/url-validation.ts)）
- Next.js Route Handler（`/api/analyze`）からPython分析API（`backend/`）を呼び出すBFF構成。未設定・失敗時は開発用ダミーデータへ自動フォールバック
- Python API側のURL本文取得（`services/web_fetcher.py`、SSRF対策込み、同時実行数3で並列取得）
- `meta.sections` によるセクション単位の実データ/ダミー/計算不能状態の可視化、`meta.documentsSource` による文章取得元の表示
- `Document`型定義（[app/lib/document.ts](../app/lib/document.ts)、`backend/models.py`）と、`user_provided`/`web_fetch`/`development_sample`すべての`Document[]`変換・共起解析への受け渡し。`meta.documentCount`/`meta.sourceTypes`として要約をAPIレスポンスに含める（development_sampleの場合も常に値が入るようになった、2026-07-16。[03_api_design.md](./03_api_design.md)、[11_architecture_v1.md](./11_architecture_v1.md)参照）
- Document Cleaner（`backend/services/document_cleaner.py`）としてHTML本文抽出・不要要素除去を`web_fetcher.py`から分離。Cookieバナー・広告らしき要素のヒューリスティック除去も含む（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- Document Normalizer（`backend/services/document_normalizer.py`、2026-07-16）として`normalize_text()`を実装。Unicode NFKC正規化（全角英数字の半角化等）・zero-width等不可視文字/制御文字の除去・タブ/連続空白/連続改行の整理・過剰な連続句読点の軽い圧縮を行う。`web_fetch`（Cleaner出力）・`user_provided`（`documents`各要素）・`development_sample`（サンプルテンプレート）すべてに適用（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- Document Chunker（`backend/services/document_chunker.py`、2026-07-16）として`Document.text`を`DocumentChunk[]`へ分割。`chunk_document()`/`chunk_documents()`。段落/改行/句読点/空白の優先順で自然な境界を探し、`overlap_chars`分だけ隣接チャンクを重ねる。`/analyze`がDocument[]から生成したチャンク件数のみ`meta.chunkCount`としてレスポンスに含める（`DocumentChunk[]`自体・チャンク本文はフロントへ返さない）
- 軽量な文脈分析（`backend/services/context_analysis.py`、2026-07-16、通称"context-analysis-lite"）として`contextAnalysis`セクションを実データ由来にした。AI/LLMは使わず、`pricing`/`feature`/`use_case`/`support`/`reliability`/`comparison`/`risk_or_issue`/`general`の8カテゴリへキーワード一致ベースで分類し、簡易センチメント（positive/neutral/negative）も判定する。ブランド名を含む`DocumentChunk`を優先、0件ならフォールバック。既存の`ContextAnalysisItem`型のまま返すため、APIレスポンス形式・UIの変更は不要だった。`meta.sections.contextAnalysis`は共起解析と同じ`"real"`/`"unavailable"`判定を共有する（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- 軽量なブランド認知サマリー（`backend/services/brand_summary.py`、2026-07-16、通称"brand-summary-lite"）として`summary`セクションを実データ由来にした。AI/LLM要約は使わず、既存の`Document[]`/`cooccurrenceRanking`/`contextAnalysis`を数える・振り分けるだけ。`totalMentions`はブランド名の出現回数の単純合計、`visibilityScore`は言及数等から0〜100を算出するMVP用の簡易推定値（development_sampleのみの場合は55点上限）、`sentimentBreakdown`は`contextAnalysis`のカテゴリ傾向から百分率化（必ず合計100%）、`topPlatforms`は実測していないChatGPT等ではなく実際の`Document.sourceType`ラベルに置き換え、`summaryText`はテンプレート文字列。既存の`BrandSummary`型のまま返すため、APIレスポンス形式・UIの変更は不要だった。`meta.sections.summary`も共起解析・文脈分析と同じ`"real"`/`"unavailable"`判定を共有する（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- 軽量な改善提案（`backend/services/improvement_suggestions.py`、2026-07-16、通称"improvement-suggestions-lite"）として`improvements`セクションを実データ由来にした。AI API・LLM・DataForSEOは使わず、既存の`cooccurrenceRanking`/`contextAnalysis`/`summary`に対する説明可能な条件分岐のみで提案を生成する（例:「pricingカテゴリが未検出のため料金・プラン情報の明確化を提案」）。`risk_or_issue`検出時は高優先度、development_sampleのみの場合は`high`優先度を`medium`にキャップ、最大5件を優先度順に返す。どのルールにも当てはまらない場合でも低優先度のフォールバック提案を1件返す（空配列にはしない）。既存の`ImprovementSuggestion`型のまま返すため、APIレスポンス形式・UIの変更は不要だった。`meta.sections.improvements`も他の3セクションと同じ`"real"`/`"unavailable"`判定を共有する（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- AI Overview比較のprovider切り替え基盤（`backend/services/ai_overview_provider.py`、2026-07-17）として`mock`（デフォルト、固定データ）/`off`（セクション無効化）/`dataforseo`（DataForSEO Sandboxへの実接続。下記参照）の3モードを導入した。環境変数`AI_OVERVIEW_PROVIDER_MODE`でデフォルトを決め、`ALLOW_AI_OVERVIEW_MODE_OVERRIDE=true`のときのみ`POST /analyze`の`aiOverviewMode`フィールドでリクエスト単位に上書きできる（デフォルトはfalseで、リクエストだけでは有効化できない安全設計）。`meta.aiOverviewProvider`（`{mode, status, reason}`、任意フィールド）で実際に使われたmodeを報告する（画面表示はまだ次タスク）。既存の`AIOverviewComparisonItem`型・APIレスポンス形状・UIの変更は不要だった（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- DataForSEO認証情報・実行安全ルールの設計（`backend/services/dataforseo_settings.py`、2026-07-17）として、`DATAFORSEO_LOGIN`/`DATAFORSEO_PASSWORD`（認証情報）・`DATAFORSEO_API_ENV`（Sandbox/Live切り替え、デフォルトsandbox）・`DATAFORSEO_LIVE_API_ENABLED`（Live API許可、デフォルトfalse）・`DATAFORSEO_REQUEST_LIMIT_PER_ANALYZE`（費用上限、デフォルト1・上限10）・Sandbox/LiveベースURLを追加した。`password`は`DataForSEOSettings`には実値を保持せず`password_configured: bool`のみを保持する設計により、ログ・レスポンス・`repr()`のいずれにも露出しない。Live API使用可否（`can_use_live_api`）は認証情報設定済み・`api_env=="live"`・`live_api_enabled`の3条件すべてが揃わないと`True`にならない設計だが、下記の通り`live`は無条件で拒否されるため現状は参照されていない。既存アカウントをMVP検証で流用する運用方針は[07_decisions.md](./07_decisions.md)に記録した（[11_architecture_v1.md](./11_architecture_v1.md)参照）
- DataForSEO **Sandbox**への実接続（`backend/services/dataforseo_client.py`、2026-07-17新設、`feature/dataforseo-sandbox-provider`）として`fetch_ai_overview_sandbox()`を実装した。**Live APIへは今回も一切接続しない**——`DATAFORSEO_API_ENV=live`の場合は`DATAFORSEO_LIVE_API_ENABLED`の値に関わらず外部APIを呼ばず`"unavailable"`を返す。認証はBasic Auth、実値を保持する新設の`DataForSEOCredentials`型（`dataforseo_settings.py`）をリクエスト直前でのみ使い保存・ログ出力しない。テストはすべて`httpx.post`をmonkeypatchで差し替え、実際のDataForSEO APIへは一切接続していない。`meta.aiOverviewProvider`の形状は変更なしのため、フロントの型・Zodスキーマの変更は不要だった。
- DataForSEO AI Mode endpointへの切り替え（`fix/dataforseo-ai-mode-endpoint`、2026-07-23）。Vercel→Render→DataForSEO Sandboxの実際の経路で手動確認したところ、当初実装した`google_organic_live_advanced`エンドポイントは`ai_overview`項目を確実には返さず`"unavailable"`のままだった。一方、DataForSEO画面から`google/ai_mode/live/advanced`を`location_code=2392`（日本）・`language_code=ja`・`device=desktop`・`os=windows`で「Vercel」を検索したところ`item_types: ["ai_overview"]`・`markdown`・`references`を含む結果が確実に得られたため、標準エンドポイントを`google_ai_mode_live_advanced`（新環境変数`DATAFORSEO_SERP_ENDPOINT`のデフォルト）へ切り替えた。`google_organic_live_advanced`は互換用に選択可能なまま残す。`DATAFORSEO_LOCATION_CODE`/`DATAFORSEO_LANGUAGE_CODE`/`DATAFORSEO_DEVICE`/`DATAFORSEO_OS`も新設し、いずれも安全なデフォルトへフォールバックする。パーサーは`rank_absolute`優先・`rank_group`フォールバック、`markdown`優先・`text`フォールバックでsummaryを作成し、`references`は`mentioned`判定にのみ使い`summary`には含めない。`platform`は`"Google AI Mode (DataForSEO Sandbox)"`に変更。**今回もLive本番ホストへは一切接続しない**。テストはすべて`httpx.post`をmonkeypatchで差し替え、実際のDataForSEO APIへは接続していない。`meta.aiOverviewProvider`の形状・フロントの型/Zodスキーマは変更不要だった（詳細は[backend/README.md](../backend/README.md)「DataForSEO Sandbox接続」、[07_decisions.md](./07_decisions.md)参照）
- DataForSEO Live API 手動確認用の安全ゲート追加（`feature/dataforseo-live-manual-gate`、2026-07-23）。DataForSEO Live本番APIへの接続を、5つの明示的な環境変数ゲート（`DATAFORSEO_API_ENV=live`・`DATAFORSEO_LIVE_API_ENABLED=true`・`DATAFORSEO_LIVE_CONFIRM_TEXT=ALLOW_DATAFORSEO_LIVE_ONCE`との完全一致・`DATAFORSEO_REQUEST_LIMIT_PER_ANALYZE=1`・認証情報設定済み）がすべて揃った場合のみ許可する「手動での1回限りの確認」経路として実装した。`backend/services/dataforseo_client.py`の`fetch_ai_overview_sandbox()`を汎用化した`fetch_ai_overview_serp(credentials, brand_name, *, api_env="sandbox", ...)`にリネームし、`api_env`引数でSandbox/Liveのどちらへ接続するかを切り替える（クライアント自体にはゲート判定ロジックを持たせず、呼び出し元の`ai_overview_provider.py`がゲート確認済みの値だけを渡す設計）。`status`だけでは区別できないSandbox成功とLive成功を見分けるため、`AiOverviewEnvironment`（`"mock"`/`"sandbox"`/`"live"`/`"off"`/`"unavailable"`）を新設し、`meta.aiOverviewProvider`に任意フィールド`environment`として追加（既存の`mode`/`status`/`reason`は維持、APIレスポンス後方互換）。フロント側（`app/lib/meta-label.ts`）は`environment`に応じて"DataForSEO Live"用の専用バッジと費用発生の可能性を明記した注意書きを表示する。テストはすべて`httpx.post`をmonkeypatchで差し替え、実際のDataForSEO API（Sandbox・Live共通）へは一切接続していない。常時のLive運用・自動スケジュール実行・複数キーワード・DB保存・課金管理は対象外（詳細は[07_decisions.md](./07_decisions.md)、[backend/README.md](../backend/README.md)「DataForSEO Sandbox/Live接続」参照）
- AI Overview本文詳細・引用元一覧の追加（`feature/ai-overview-details-references`、2026-07-23）。DataForSEO成功時（Sandbox/Live共通）の`AIOverviewComparisonItem`に任意フィールド`fullSummary`（本文の長め抜粋、最大2500文字）・`references`（引用元一覧、`title`/`domain`/`url`/`text`/`source`/`position`、urlまたはdomain+titleで重複排除・最大10件）・`ownDomainReferenced`（リクエストの`urls`のドメインが`references`と一致するかの単純な文字列比較、`urls`未指定時は`null`）を追加した。既存の`platform`/`mentioned`/`rank`/`summary`は変更なし、3フィールドとも任意のためmock/off/既存レスポンスへの影響はない。**DataForSEOレスポンスの生データ全文はこれまで通り一切返さない**。フロント（`AIOverviewComparisonSection.tsx`）に本文詳細の折りたたみ表示（`<details>`）・参照元一覧・自社ドメイン参照有無の表示を追加した（表示ロジックは`app/lib/meta-label.ts`の`getAiOverviewItemDetailDisplay()`）。Live API gate・Render設定・API呼び出し回数は変更していない。テストはすべて`httpx.post`をmonkeypatchで差し替え、実際のDataForSEO APIへは一切接続していない（詳細は[backend/README.md](../backend/README.md)「DataForSEO Sandbox/Live接続」参照）
- AI Overview比較セクションの1カラムカード型レイアウトへの変更（`style/ai-overview-card-layout`、2026-07-23、UIのみ）。DataForSEO Sandbox確認で長文の`fullSummary`・`references`を表示した際、テーブル形式では横スクロールが発生し読みにくかったため、`app/components/sections/AIOverviewComparisonSection.tsx`の`<table>`を撤去し、platform/rank/mentioned/summary/fullSummary/referencesを縦積みで表示する1カラムのカード（`<article>`、`rounded-xl border`）へ変更した。`break-words`/`whitespace-pre-wrap`/`min-w-0`等で長い英語文・URL・ドメインの折り返しを改善し、参照元一覧は番号付きリストでdomain/title中心（URL全文は表示せずhrefのみ）にした。mock/sandbox/live/unavailableのprovider状態表示は変更なし。**backend・APIレスポンス形式・DataForSEO呼び出し条件・Live API手動確認用ゲートはいずれも変更していない**（UIのみの変更）。テストは`app/lib/meta-label.ts`の`getAiOverviewItemDetailDisplay()`に対する既存ロジックテストを拡張（React Testing Library未導入のため、カードが必要とするデータが揃うことをロジックレベルで確認）
- AI Overview比較セクションの外側コンテナ幅の拡大（`style/widen-ai-overview-section`、2026-07-23、UIのみ）。`app/components/AnalysisDashboard.tsx`のグリッド上で、`BrandSummarySection`と同じ`<div className="lg:col-span-2">`ラッパーを`AIOverviewComparisonSection`にも適用し、PC幅（`lg`以上）では他の短いセクションの2倍の横幅を使うようにした。スマホ幅（`lg`未満）は元々1カラムグリッドのため変化なし。カード型表示自体（`AIOverviewComparisonSection.tsx`）は変更していない。**backend・API・DataForSEO・Live gate・Render/Vercel環境変数はいずれも変更していない**
- AI Overview参照元の簡易分類・改善提案への反映（`feature/ai-overview-reference-classification`、2026-07-23）。DataForSEO Liveで日本語ブランド（サイボウズ）でも成功が確認できたことを受け、`AIOverviewReference`に任意フィールド`category`（`official`/`wikipedia`/`sns`/`ugc`/`news`/`media`/`video`/`other`）、`AIOverviewComparisonItem`に任意フィールド`referenceSummary`（`{total, official, thirdParty, categories}`）を追加した。分類は既存の`references`とリクエストの`urls`だけを使うルールベース判定（自社ドメイン一致→`official`、Wikipedia/SNS/UGC/動画/ニュースの小さなdomainリストとの照合、それ以外は`other`）で、**新たなDataForSEO呼び出しは一切しない**。UIには「参照元の内訳」（自社/第三者件数・主な分類）とカテゴリバッジを追加した。さらに`build_improvement_suggestions()`に`ai_overview_items`引数を追加し、`ownDomainReferenced`/`referenceSummary`から改善提案を最大1件反映するようにした（mock/off/unavailable時は追加なし）。既存フィールド・Live API gate・Render環境変数は変更していない
- AI Overview参照元分類のUI表示改善（`style/reference-summary-ui`、2026-07-23、UIのみ）。日本語ブランド（サイボウズ）でのLive確認で`referenceSummary`の実データ（合計10件・自社公式5件・第三者5件・カテゴリ内訳）を得たことを受け、テキスト中心だった「参照元の内訳」を、合計/自社公式/第三者の3枚のミニカードと、カテゴリ別件数のバッジ表示に変更した（`app/lib/meta-label.ts`の`categoryCounts`、`AIOverviewComparisonSection.tsx`）。`ownDomainReferenced`の表示文言も「自社公式サイトが...」へ明確化し、色分けした強調表示にした。**backend・API・DataForSEO・Live gate・Render/Vercel環境変数はいずれも変更していない**（UIのみ、新規ライブラリ追加なし）
- 開発・検証用のAI Overview取得モード選択UI（`feature/ai-overview-mode-selector`、2026-07-23）。ブラウザConsoleからの手動fetchによるSandbox/Live確認の手間を減らすため、Next.js側の新規環境変数`NEXT_PUBLIC_ENABLE_AI_OVERVIEW_MODE_SELECTOR`（デフォルト`false`）が`true`の場合のみ、分析フォームに「AI Overview取得モード（検証用）」というmock/off/dataforseoのselectを表示するようにした（`app/components/BrandInputForm.tsx`、`app/lib/analysis-request.ts`新設）。選択値は`aiOverviewMode`としてリクエストボディに含まれるが、**これはUI表示のみを制御するフラグであり、実際に上書きが適用されるかは既存のPython API側`ALLOW_AI_OVERVIEW_MODE_OVERRIDE`が、DataForSEO Live APIの実行可否はさらに既存の5つの手動確認用ゲートが、それぞれ引き続き決定する**。未設定/`false`では選択UIは表示されず`aiOverviewMode`も送られない（既存挙動を完全維持）。`backend/`・DataForSEO呼び出し条件・Live API gate・Render/Vercel既存環境変数はいずれも変更していない
- ChatGPT相当モデルの1問観測（`feature/chatgpt-observation-provider`、2026-07-23）。依頼者への提出に向けた暫定対応として、AI Overview比較へOpenAI APIを使ったChatGPT相当モデルの観測結果を追加できるようにした。**これはChatGPTアプリ画面そのものの内部認識を再現するものではなく**、OpenAI APIのモデルへブランド名の一般的な認知のされ方を1問だけ質問し、その回答を「ChatGPT相当モデルの観測結果」として表示するものである（Web検索・参照元付き回答は使わない）。新規モジュール`backend/services/chatgpt_settings.py`（`OPENAI_API_KEY`/`CHATGPT_MODEL`/`CHATGPT_MAX_OUTPUT_TOKENS`/`CHATGPT_REQUEST_LIMIT_PER_ANALYZE`）・`chatgpt_client.py`（`httpx`によるOpenAI Responses API（`POST https://api.openai.com/v1/responses`）への直接REST接続、`openai` SDKは追加していない）・`chatgpt_provider.py`（`CHATGPT_PROVIDER_MODE`環境変数（デフォルト`off`）＋`ALLOW_CHATGPT_MODE_OVERRIDE`環境変数（デフォルト`false`）の2段階ゲート、DataForSEOと同じ設計）を追加した。`OPENAI_API_KEY`未設定・`CHATGPT_REQUEST_LIMIT_PER_ANALYZE!=1`のいずれかがあれば外部APIを一切呼ばず、1 `/analyze`あたり最大1回に固定。成功時は既存の`AIOverviewComparisonItem`型を再利用し（`platform: "ChatGPT (OpenAI API)"`、`rank`/`references`/`referenceSummary`/`ownDomainReferenced`は`None`固定）、`aiOverviewComparison`へ1件追加する——これらのフィールドはすべて元々任意だったため、`AIOverviewComparisonSection.tsx`・`meta-label.ts`の変更は不要だった。**既存の固定`mock`フィクスチャに元から「ChatGPT」という名前のダミーカードが含まれているため、`aiOverviewMode`が`"mock"`の場合はChatGPT観測自体を呼び出さずスキップして重複を避ける**設計にした。`meta.chatgptProvider`（`{mode, status, reason, environment}`）を`meta.aiOverviewProvider`とは完全に独立して追加。フロントには`NEXT_PUBLIC_ENABLE_CHATGPT_MODE_SELECTOR`（デフォルト`false`）が`true`の場合のみ表示される検証用のChatGPT観測モードselectを追加した（`chatgptMode`はUI表示時のみリクエストボディに含める）。**OpenAI APIキーはRender Environment Variablesにのみ設定し、GitHub・フロントエンドには一切露出させない**（`reason`・ログ・レスポンスにも含めない）。**DataForSEO関連コード・Live API gate・DataForSEO呼び出し条件は変更していない**。詳細は[07_decisions.md](./07_decisions.md)「2026-07-23 — ChatGPT相当モデルの1問観測を...」、[11_architecture_v1.md](./11_architecture_v1.md)参照
- AI Overview比較カードの「続きを見る」表示（`style/ai-overview-continuation-display`、2026-07-28、UIのみ）。ChatGPT観測機能の追加後、回答が短めの場合に「概要」（summary）と「詳細を見る」（fullSummary全文）がほぼ同じ内容に見える重複表示の問題が見つかったための改善。backend・API・DataForSEO・OpenAI providerは一切変更せず、`app/lib/meta-label.ts`の表示用helperのみで対応した。`getAiOverviewItemDetailDisplay()`の`hasDetail`/`detailText`を`hasContinuation`/`continuationText`にリネームし、新設の`buildContinuationText()`がsummaryとfullSummaryの共通の先頭部分を取り除いた続き部分だけを抽出する（空白・改行の差分を吸収するため比較時のみ連続空白を1つに正規化し、summary末尾の`…`/`...`を除去してから前方一致を判定。一致した場合は正規化後の文字数から元のfullSummary側の対応位置を逆算し、改行を保ったまま続き部分を切り出す）。続き部分が30文字未満、またはfullSummaryがsummaryとほぼ同じ場合は`hasContinuation: false`にしてdetailsごと非表示にする。前方一致が取れない場合（markdownクリーニングの差異等）は簡易フォールバックとして十分に長ければfullSummary全体を続きとして表示する（完全なdiffアルゴリズムは実装せず簡易ヒューリスティックにとどめた）。`app/components/sections/AIOverviewComparisonSection.tsx`のラベルを「詳細を見る」から「続きを見る」に変更。references/referenceSummary/ownDomainReferenced表示・DataForSEOカードの既存表示は変更していない。**APIレスポンス形式・summary/fullSummary生成ロジック（`dataforseo_client.py`/`chatgpt_client.py`）・Live API手動確認用ゲート・Render/Vercel環境変数はいずれも変更していない**。テストは`app/lib/meta-label.test.ts`に新しい`describe`ブロックを追加（前方一致抽出・改行/空白正規化・`...`/`…`除去・最小文字数閾値・フォールバック挙動・既存表示への非影響を確認）
- ChatGPTカードで「続きを見る」が出ない時のsummary末尾省略記号の除去（`style/summary-ellipsis-without-continuation`、2026-07-28、UIのみ）。ChatGPT (OpenAI API)カードでsummary/fullSummaryの長さが近いケース（例: summary 201文字・fullSummary 214文字）が確認され、上記「続きを見る」の設計上`hasContinuation`が`false`になる一方、backend側で短縮された`summary`の末尾に`…`/`...`が残ったままになり「省略されているのに続きを開けない」ように見える問題を修正した。backend・API・OpenAI・DataForSEO providerは一切変更せず、`app/lib/meta-label.ts`の表示用helperのみで対応。`getAiOverviewItemDetailDisplay()`に`displaySummary: string`を追加し、`hasContinuation === true`なら`item.summary`をそのまま（末尾の省略記号も残す）、`hasContinuation === false`なら新設`stripTrailingEllipsisForDisplay()`（既存の比較用`stripTrailingEllipsis()`に`.trim()`を足した表示用ラッパー）で末尾の`…`/`...`のみを取り除く（文中の三点リーダーは対象外）。`AIOverviewComparisonSection.tsx`の概要表示を`item.summary`から`detail.displaySummary`参照に変更。references/referenceSummary/ownDomainReferenced表示・DataForSEOカードの既存表示・APIレスポンス形式・summary/fullSummary生成ロジックはいずれも変更していない。テストは`app/lib/meta-label.test.ts`に新しい`describe`ブロックを追加（continuation有無での末尾記号の扱い分け、`…`/`...`双方の除去、文中の`…`は非除去、通常文は無変更、既存表示への非影響を確認）
- ChatGPT観測回答のデモ向け安定化（`fix/chatgpt-observation-stability`、2026-07-28）。同じブランド名でもChatGPT (OpenAI API)観測の回答・summary/fullSummaryの長さが実行ごとにばらつく課題を受け、依頼者への提出デモに向けて回答を安定させた。新規環境変数`CHATGPT_TEMPERATURE`（`backend/services/chatgpt_settings.py`、デフォルト`0.2`、許可範囲0.0〜1.0、範囲外・不正値は`0.2`にフォールバック）を追加し、OpenAI Responses APIのリクエストボディに`temperature`を含めるようにした（`store: false`は維持）。system/userプロンプトも構造化し、systemプロンプトは日本語化してWeb検索禁止・簡潔な回答を指示、userプロンプトは「何を提供しているか／主な利用者または用途／代表的な特徴や強み」を含む3〜5文程度の自然文（箇条書き禁止、参照元・URLなし）で回答するよう明示的に指示するようにした。**OpenAI API呼び出し回数（1 analyzeあたり最大1回）・ChatGPT providerの2段階安全ゲート・APIキーの扱い・DataForSEO関連コード・Live API手動確認用ゲートはいずれも変更していない**。テストは`test_chatgpt_settings.py`/`test_chatgpt_client.py`/`test_chatgpt_provider.py`にtemperature関連のケースを追加。詳細は[backend/README.md](../backend/README.md)「ChatGPT相当モデルの1問観測」参照
- 短いAI Overview本文の全文表示・長文のみ「続きを見る」化（`style/inline-short-ai-overview-summary`、2026-07-28、UIのみ）。ChatGPT (OpenAI API)カードで、概要が「Next....」のように途中で切れ、「続きを見る」を開くと文の続き（「jsの主要な開発元／メンテナーとしての位置付けでも認識されています。」等）が挟まって見える読みにくさを解消した。backend・API・OpenAI・DataForSEO providerは一切変更せず、`app/lib/meta-label.ts`のみで対応。全文表示しきい値`INLINE_FULL_SUMMARY_MAX_LENGTH`（600文字）を新設し、`fullSummary`が600文字以下ならそれを`displaySummary`としてそのまま全文表示（`hasContinuation`は常に`false`、「続きを見る」自体を出さない）、600文字超（または`fullSummary`なし）の場合のみ従来どおりsummary + continuation-toggle表示にフォールバックする。既存の30文字未満の続き非表示ルール・省略記号除去ロジック・ChatGPT/DataForSEO両カードへの適用は維持している。`AIOverviewComparisonSection.tsx`は変更不要。テストは`app/lib/meta-label.test.ts`に新しい`describe`ブロックを追加し、600/601文字の境界値・長文フォールバック・DataForSEOカード相当の参照元表示への非影響を確認した。詳細は[05_tasks.md](./05_tasks.md)参照
- AI Overview取得モードへの`dataforseo_sandbox`/`dataforseo_live`明示モード追加（`feature/explicit-dataforseo-live-mode`、2026-07-28）。従来の`dataforseo`モードは`DATAFORSEO_API_ENV`環境変数でSandbox/Liveを切り替える設計で、画面上ではどちらが実行されるか分かりにくかった課題を解消した。`dataforseo_sandbox`は`DATAFORSEO_API_ENV`の値に関わらず常にSandboxへ接続（Liveゲート不要）、`dataforseo_live`は明示的にLiveを要求するが既存の5つの手動確認用ゲート（`DATAFORSEO_API_ENV=live`・`DATAFORSEO_LIVE_API_ENABLED=true`・`DATAFORSEO_LIVE_CONFIRM_TEXT=ALLOW_DATAFORSEO_LIVE_ONCE`・`DATAFORSEO_REQUEST_LIMIT_PER_ANALYZE=1`・認証情報設定済み）がすべて揃わない限りLiveへ接続しない。ゲート不足時は「DataForSEO Live mode was requested, but ...」で始まる不足条件が分かる理由文言（credentials非露出）を返す。**`dataforseo_settings.py`/`dataforseo_client.py`のコアゲート判定ロジックは変更なし**——新モードは既存の`is_live_allowed_for_manual_check`等を組み合わせて呼ぶだけ。既存の`dataforseo`モード（env駆動）は後方互換として維持。検証用の「AI Overview取得モード（検証用）」selectorに「DataForSEO Sandbox」「DataForSEO Live」の選択肢を追加し、Live選択時のみ課金注意文を表示する（`app/components/BrandInputForm.tsx`）。ChatGPT (OpenAI API)観測との併用も確認済み。詳細は[backend/README.md](../backend/README.md)「AI Overview比較のprovider mode」参照
- gpt-5系モデルではtemperatureを送らないChatGPT観測の安全化（`fix/omit-temperature-for-gpt5-chatgpt-observation`、2026-07-28）。`CHATGPT_MODEL=gpt-5-mini`＋`CHATGPT_TEMPERATURE=0.2`でOpenAI Responses APIがHTTP 400を返しChatGPTカードが表示されない不具合を修正した（`gpt-4.1-mini`では同じ`temperature`値で正常動作していたため、gpt-5系への`temperature`送信が原因と判断）。`backend/services/chatgpt_client.py`に`should_send_temperature(model)`ヘルパーを追加し、モデル名（大文字小文字を区別しない）が`"gpt-5"`から始まる場合のみリクエストボディから`temperature`キーを省略するようにした。`CHATGPT_TEMPERATURE`環境変数・`chatgpt_settings.py`のバリデーションロジックは変更なし（モデルに関わらず引き続き読み取られる。送信可否だけを`chatgpt_client.py`が判断）。`model`/`input`/`max_output_tokens`/`store: false`は変更していない。**DataForSEO関連コード・ChatGPT provider mode・request limit・promptはいずれも変更していない**。デモの現行推奨は引き続き`gpt-4.1-mini`だが、将来`gpt-5-mini`に戻してもHTTP 400にならないことを確認済み。詳細は[backend/README.md](../backend/README.md)「ChatGPT相当モデルの1問観測」参照
- Common Crawl最小連携の設計docsを追加（`docs/common-crawl-mvp-design`、2026-07-28、docsのみ・コード変更なし）。新規[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)を作成し、ブランド名の全文検索ではなくdomain指定でのCommon Crawl Index検索から始める最小MVP方針を整理した。既存Document Pipeline（Provider→Cleaner→Normalizer→Chunker→Analyzer）への接続方法、Provider設計案、Environment Variables案（すべて初期値off/安全側）、API設計案（`/analyze`統合前にservice層+テストから始める推奨順序）、失敗時の扱い、安全制限、10段階の実装ステップを明文化。`docs/02_roadmap.md`のPhase 3-2にCommon Crawl連携のCurrent/Next/Laterを反映した。実装（Index検索クライアント・WARC取得・`Document[]`統合・UI連携）はこれから段階的に着手する
- Common Crawl settings + Index API clientを実装（`feature/common-crawl-index-client`、2026-07-28）。上記設計docsの実装ステップ2〜4（settings・Index API client・Index検索のみのテスト）にあたる、**Index API検索クライアントのみ**の実装。新規`backend/services/common_crawl_settings.py`（`COMMON_CRAWL_ENABLED`/`_INDEX`/`_MAX_RESULTS`/`_TIMEOUT_SECONDS`/`_USER_AGENT`、デフォルトはすべて安全側=off。Common Crawlは認証不要のためcredential型なし）・`backend/services/common_crawl_index.py`（`resolve_common_crawl_index()`が`collinfo.json`から最新indexを解決、または明示indexをそのまま使用（明示時はcollinfo.jsonへ一切アクセスしない）。`search_common_crawl_domain()`がdomainを正規化・検証し、Common Crawl Index API（`index.commoncrawl.org`）へdomain指定検索（`filter=status:200`・`filter=mime:text/html`・`limit=max_results`）を行い、JSON Linesレスポンスを`CommonCrawlCandidate`へ正規化）を新設した。**このクライアントは`COMMON_CRAWL_ENABLED`によるゲート判定を持たず、DataForSEO/ChatGPTクライアントと同じ「ゲートはproviderレイヤーの責務」という設計を踏襲**しており、`common_crawl_provider.py`相当は今回未実装のため`COMMON_CRAWL_ENABLED`はまだどのコードパスにも作用しない。**WARC本文取得・HTML抽出・`Document[]`化・`/analyze`統合・UI追加は今回のスコープ外で未実装のまま**。ネットワークエラー・非200・不正JSON・0件はいずれも例外を送出せず`"unavailable"`＋安全な`reason`になり、HTML/WARC本文・巨大なレスポンス本文は一切保持しない。テストは`test_common_crawl_settings.py`（27件）・`test_common_crawl_index.py`（25件）を新設し、すべて`httpx.get`をmonkeypatchで差し替え、実際のCommon Crawlへは接続していない。**DataForSEO/ChatGPT関連コード・`/analyze`のレスポンス形状・frontend（`app/`）は変更していない**。詳細は[backend/README.md](../backend/README.md)「Common Crawl最小連携」、[05_tasks.md](./05_tasks.md)参照
- Common Crawl WARC fetch / HTML extraction serviceを実装（`feature/common-crawl-warc-fetch`、2026-07-28）。前記の`CommonCrawlCandidate`（`filename`/`offset`/`length`）を使い、**最大1件だけ**WARCレコードをRange requestで取得しHTML本文を抽出する新規`backend/services/common_crawl_warc.py`（`fetch_common_crawl_warc_record()`）を追加した。`https://data.commoncrawl.org/{filename}`へ`Range: bytes={offset}-{offset+length-1}`でGETし（`length`がモジュール内定数`MAX_WARC_RANGE_BYTES`＝1,500,000バイト超はHTTPを呼ばず`unavailable`）、`gzip.decompress()`で展開（新規依存なし、展開後`MAX_DECOMPRESSED_BYTES`＝8,000,000バイト超も`unavailable`）、WARCヘッダー/HTTPヘッダー/HTML bodyを空行境界で分離し、`Content-Type`が`text/html`/`application/xhtml+xml`以外は`unavailable`、charsetが判別できればそれで・できなければ`utf-8`でデコードする。抽出後HTMLは`MAX_HTML_CHARS`＝200,000文字で切り詰め（拒否はしない）。**このモジュールも`COMMON_CRAWL_ENABLED`によるゲート判定を持たない**（前記クライアントと同じ設計）。**複数件のWARC取得・`Document[]`化・既存Cleanerとの連携・`/analyze`統合・UI追加は今回のスコープ外で未実装のまま**。失敗時はいずれも例外を送出せず`unavailable`＋安全な`reason`になり、生のWARCバイト列・HTML本文・巨大なレスポンス本文は`reason`はもちろんどのフィールドにも保持しない。テストは`test_common_crawl_warc.py`（30件）を新設し、すべて`httpx.get`をmonkeypatchで差し替え、実際のCommon Crawlへは接続していない。**DataForSEO/ChatGPT関連コード・`/analyze`のレスポンス形状・frontend（`app/`）は変更していない**。詳細は[backend/README.md](../backend/README.md)「Common Crawl最小連携」、[05_tasks.md](./05_tasks.md)参照
- Common Crawl `Document[]`変換serviceを実装（`feature/common-crawl-document-provider`、2026-07-28）。前記の`CommonCrawlCandidate`＋`CommonCrawlFetchResult`を、既存のDocument Pipeline（[11_architecture_v1.md](./11_architecture_v1.md)「4. Document Pipeline」）が扱える`Document`型（`sourceType: "common_crawl"`）へ変換する新規`backend/services/common_crawl_document_provider.py`（`build_common_crawl_document()`/`build_common_crawl_documents()`）を追加した。HTML本文は既存の`document_cleaner.clean_html_to_text()`→`document_normalizer.normalize_text()`（いずれも変更なし、Common Crawl専用HTML parserは追加していない）を経て`Document.text`になる。`metadata`には`provider`/`crawlIndex`/`warcFilename`/`warcOffset`/`warcLength`/`warcTimestamp`/`mime`/`status`/`digest`/`fetchedBytes`/`contentType`を格納する。`fetch_result.status != "real"`・HTML欠落・クリーニング後の本文が空、いずれも`unavailable`。**Common Crawl検索→WARC取得→Document化までを自動でつなぐ処理・複数件の一括fetch・`/analyze`統合・UI追加は今回のスコープ外で未実装のまま**——Common Crawl由来の`Document`はまだ分析結果（`cooccurrenceRanking`等）には一切反映されない。テストは`test_common_crawl_document_provider.py`（24件）を新設（既存`sample_documents`/`web_fetcher`のDocument providerが壊れていないことのリグレッション確認も含む）。**DataForSEO/ChatGPT関連コード・`/analyze`のレスポンス形状・frontend（`app/`）は変更していない**。UI追加段階で依頼者に確認すべき表示名・説明文の暫定方針を[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「11. 依頼者確認が必要な点」にメモした。詳細は[backend/README.md](../backend/README.md)「Common Crawl最小連携」、[05_tasks.md](./05_tasks.md)参照
- Common Crawlを`/analyze`に最小統合（UIなし）（`feature/common-crawl-analyze-integration`、2026-07-28）。`backend/models.py`の`AnalyzeRequest`に`commonCrawlMode`（`"off"`デフォルト/`"domain"`）・`commonCrawlDomain`を追加し、`backend/main.py`の新設`_build_common_crawl_documents()`が、指定domain（未指定時は`urls[0]`のホスト名）に基づくCommon Crawl補完Documentを**最大1件**（最大3候補まで試行）既存のDocument[]へ追加できるようにした。追加は共起解析・チャンク化・文脈分析・ブランド認知サマリー・改善提案の**いずれよりも前**に行うため、他のDocumentと全く同じ経路でAnalyzerに渡る。**`COMMON_CRAWL_ENABLED=false`の場合、`commonCrawlMode="domain"`を指定しても一切接続しない**（`aiOverviewMode`/`chatgptMode`と異なり`ALLOW_*_OVERRIDE`のような追加env gateは設けず、単一の`COMMON_CRAWL_ENABLED`ゲートのみで判定する設計をタスクの初期方針として採用）。Common Crawlが失敗しても（domain未決定・Index検索失敗/0件・WARC fetch失敗・Document変換失敗のいずれも）`/analyze`全体は継続し、既存の解析には一切影響しない。新設`CommonCrawlProviderInfo`（`mode`/`status`/`reason`/`domain`/`crawlIndex`/`candidateCount`/`documentCount`）を`meta.commonCrawlProvider`として追加（HTML本文・WARC本文・生レスポンスは含まない）。フロント側は`app/lib/types.ts`/`analysis-result-schema.ts`/`app/api/analyze/route.ts`を拡張し、`commonCrawlMode`/`commonCrawlDomain`をそのままbackendへ中継できるようにしたが、**UI（`BrandInputForm.tsx`）にはまだ追加していない**——API/Console検証専用。テストは`test_main.py`に12件・`route.test.ts`に6件追加。前タスクで追加された「`main.py`に参照がないこと」の回帰防止テスト2件は、本タスクで意図的に統合したため前提が崩れ削除した。**DataForSEO/ChatGPT関連コード・frontend UIコンポーネント・環境変数の新規追加はいずれも行っていない**。詳細は[backend/README.md](../backend/README.md)「Common Crawl最小連携」、[05_tasks.md](./05_tasks.md)参照
- Common Crawl補完のUI selectorを追加（`feature/common-crawl-ui-selector`、2026-07-28）。既存のAI Overview/ChatGPT検証用selectorと同じ`NEXT_PUBLIC_ENABLE_*_SELECTOR`パターンで`NEXT_PUBLIC_ENABLE_COMMON_CRAWL_MODE_SELECTOR`（デフォルトfalse）を新設し、`app/components/BrandInputForm.tsx`に「Common Crawl補完（検証用）」off/domain selectorと、domain選択時のみ表示される任意のドメイン入力欄を追加した（未入力時はbackendの`urls[0]`フォールバックにそのまま任せ、frontend側で厳しいvalidationはしない）。表示名・helper text・warning textは`BrandInputForm.tsx`の`COMMON_CRAWL_UI_TEXT`定数にまとめ、依頼者確認前の仮の文言として管理する（[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「11. 依頼者確認が必要な点」参照）。`app/lib/analysis-request.ts`の`buildAnalyzeRequestBody()`に`commonCrawlMode`/`commonCrawlDomain`を追加（既存フィールドと同じ「省略時はデフォルト扱い」パターン）、`app/page.tsx`はこれらを`BrandInputForm`から受け取り中継するよう拡張した。`meta.commonCrawlProvider`の状態は新設`app/lib/meta-label.ts`の`getCommonCrawlProviderDisplay()`が「Common Crawl補完: オフ／取得済み（N件）／未取得（理由: ...）」という軽い状態文にまとめ、「2. 共起語ランキング」カード（`CooccurrenceRankingSection.tsx`）に表示する——**WARC metadata・HTML本文・WARC生バイト列はいずれも表示しない**（`CommonCrawlProviderInfo`にそのためのフィールドが存在しない）。`app/api/analyze/route.ts`は前タスクで既にパススルー対応済みのため変更不要。テストは`app/lib/analysis-request.test.ts`（13件）・`app/lib/meta-label.test.ts`（7件）を追加した。**このプロジェクトには`@testing-library/react`等のReactコンポーネント描画テスト基盤が存在せず、`package.json`変更は本タスクの対象外のため新規導入もしていない**——`BrandInputForm.tsx`のselector表示切り替え・domain入力欄の条件付き表示自体は、既存のAI Overview/ChatGPT selectorと同様にコンポーネントレベルの自動テスト対象外のまま（このプロジェクト全体に共通する既存の制約であり、本タスク固有の欠落ではない）。**DataForSEO/ChatGPT関連コード・Common Crawl backend service（`common_crawl_index.py`/`common_crawl_warc.py`/`common_crawl_document_provider.py`）は変更していない**。詳細は[backend/README.md](../backend/README.md)「Common Crawl最小連携」の「フロントエンドUI selector」、[05_tasks.md](./05_tasks.md)参照
- Common Crawl補完の表示文言を整える（`style/common-crawl-source-labels`、2026-07-28）。実環境確認で、ブランド認知サマリー（`summary.topPlatforms`）にCommon Crawl統合完了後も残っていた「Common Crawl（未実装）」という不正確な表示を発見・修正した。`backend/services/brand_summary.py`の`_SOURCE_TYPE_LABELS`の`common_crawl`ラベルを「Common Crawl補完」に変更し、UI selectorの表記と統一した。あわせて`app/components/sections/BrandSummarySection.tsx`の見出しを「主要プラットフォーム」→「分析ソース」に変更（Common Crawl/Webページ/入力テキスト等の異質な入力ソースが混在し得るため）。Common Crawlがoff/未取得の場合は`sourceType: "common_crawl"`のDocument自体が作られないため、既存の`_build_top_platforms()`ロジックのまま分析ソース欄に表示されない（新たな分岐追加は不要）。共起語ランキング側のCommon Crawl状態表示（`getCommonCrawlProviderDisplay()`）も、取得成功時のドメイン/index情報を1行の括弧書きから「対象ドメイン: .../Index: ...」という2行目に分離して読みやすくした。テストは`backend/tests/test_brand_summary.py`に3件追加、`test_main.py`の既存Common Crawl成功系テストに`topPlatforms`アサーションを追加、`app/lib/meta-label.test.ts`を新表記に更新。**「分析ソース」見出し自体の変更にはコンポーネント描画テスト基盤が無いため直接のテストは無い**（既存selectorと同様の制約）。**DataForSEO/ChatGPT関連コード・Common Crawl backend service・`/analyze`仕様は変更していない**。詳細は[backend/README.md](../backend/README.md)「ブランド認知サマリー」「Common Crawl最小連携」参照
- ステージング環境ヘッダーの注意書きを最新版に更新（`style/update-staging-banner-copy`、2026-07-28）。画面ヘッダーのバナー文言が「共起語ランキングのみ実データ計算、その他のセクションは開発用データです。Common Crawl・DataForSEOとの連携はまだ行っていません。」という、実装の進捗と合わなくなった古い内容のまま残っていた問題を修正した。新規`app/lib/staging-banner.ts`に`STAGING_BANNER_TEXT`定数を切り出し（コンポーネント描画テストが無いプロジェクトでも文言を直接ユニットテストできるようにするため）、`app/page.tsx`はこれを参照するだけに変更した。新しい文言は「この環境は開発中の依頼者確認用ステージング環境です。機密情報・個人情報・本番データは入力しないでください。URL解析、Common Crawl補完、DataForSEO連携、ChatGPT観測など一部の機能は実データまたは外部APIを用いて検証できますが、分析結果は開発中の推定表示を含みます。」——ステージング環境である旨・機密情報等を入力しないでほしい旨は維持しつつ、Common Crawl/DataForSEO/ChatGPTが実データ/外部APIで検証可能になったことを明記し、`visibilityScore`・改善提案等が依然としてルールベースのMVP推定であることも併記した。ルート[README.md](../README.md)の要約・[09_deployment.md](./09_deployment.md)の記載も同様に更新した（あわせて、`09_deployment.md`が誤って「backend/README.mdに同内容を記載」としていた不正確な記述も訂正——backend/README.mdはもともと別の短い注意書きしか持っていなかった）。テストは`app/lib/staging-banner.test.ts`（6件）を新設。**DataForSEO/ChatGPT/Common Crawl backend service・`/analyze`仕様は変更していない**。詳細は[09_deployment.md](./09_deployment.md)「6. 確認用環境であることの明示」参照
- 共起語ランキングのノイズ語を減らす（`fix/cooccurrence-noise-filter`、2026-07-28）。Common Crawl補完を有効化した実環境で、共起語ランキングに「には」「くことが」「しくなる」のような機能語断片が上位表示される問題を発見・修正した。原因は共起語抽出のデフォルト`simple`トークナイザー（`backend/services/cooccurrence.py`）がひらがな/カタカナ/漢字の文字種境界でしか単語を区切れないこと。既存`STOPWORDS`へ「には」「では」「ことが」「しくなる」等を追加し、両トークナイザー共通の新しい第二段フィルタ`is_low_value_cooccurrence_term()`（固定stopwords一致・接尾辞ベースの除外`NOISE_SUFFIXES`・4文字以下の完全ひらがな語除外）を追加した。「サイト」「デジタル」「導入事例」「グループウェア」等の意味のある語は除外されないことを確認済み。**Common Crawl由来Documentへの特別な分岐は追加していない**（共起語抽出はDocumentの取得元を一切見ないため、既存の仕組みのまま全Documentが同じフィルタを通る）。テストは`test_cooccurrence_simple.py`に11件・`test_cooccurrence.py`に1件を追加、既存テストは無変更で通過。**新規形態素解析ライブラリ・AI分類は導入していない。UI/API仕様・Common Crawl取得処理（`common_crawl_index.py`/`common_crawl_warc.py`/`common_crawl_document_provider.py`）・DataForSEO/ChatGPT関連コードは変更していない**。詳細は[backend/README.md](../backend/README.md)「共起語ランキングのノイズ語フィルタ」参照
- Common Crawl補完を最大1件から最大3件へ拡張（`feature/common-crawl-multiple-documents`、2026-07-28）。実環境で1件取得できることを確認できたため、安全制限を維持したまま複数件取得できるようにした。`backend/main.py`の`_build_common_crawl_documents()`のみを変更し、Common Crawl service層（`common_crawl_index.py`/`common_crawl_warc.py`/`common_crawl_document_provider.py`）は無変更。新設した`COMMON_CRAWL_MAX_CANDIDATES_TO_TRY`（5、試行する候補数の上限）と`COMMON_CRAWL_MAX_DOCUMENTS_PER_ANALYZE`（3、追加するDocumentの上限）に基づき、候補を順に試して失敗した候補はスキップし、成功が3件に達したら打ち切る（5件すべて試しても3件に届かない場合は集まった分だけ採用）。一部成功時は`reason`に`"Common Crawl completed with partial results (...)"`という新しい文言を使う。Common Crawlが一部失敗・全件失敗しても`/analyze`全体は成功する。フロントエンドの`getCommonCrawlProviderDisplay()`は元々`documentCount`をそのまま表示する実装だったためコード変更不要（表示テストのみ追加）。テストは`test_main.py`に7件・`meta-label.test.ts`に3件追加。**UI・DataForSEO/ChatGPT関連コード・環境変数・Render/Vercel設定は変更していない**。詳細は[backend/README.md](../backend/README.md)「複数件取得への拡張」、[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「13. 複数件取得への拡張」参照
- 分析ソース内訳表示を追加（`style/analysis-source-breakdown`、2026-07-28）。Common Crawl補完が最大3件取得できるようになった実環境で、「URL取得: N/M件成功」「Common Crawl補完: 取得済み（N件）」という既存の状態表示が別々の行で分かりにくかったため、新規`app/lib/meta-label.ts`の`getAnalysisSourceBreakdownDisplay()`が既存の`meta.urlFetchResults`/`meta.commonCrawlProvider`（`status: "real"`のみ）から「Webページ N件 / Common Crawl補完 N件」という1行の内訳を組み立て、「共起語ランキング」カードへ表示する。**backend側の変更は一切無い**（既存response schemaだけで表現可能なfrontend専用実装）。Common Crawlがoff/unavailableの場合は内訳にCommon Crawlを含めない。テストは`meta-label.test.ts`に7件追加。**Common Crawl取得ロジック・取得件数・改善提案ロジック・DataForSEO/ChatGPT関連コードは変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「14. 分析ソース内訳表示」参照
- Common Crawl由来データを改善提案へ反映する方針docsを追加（`docs/common-crawl-improvement-policy`、2026-07-28）。Common Crawl補完は取得・分析入力への統合（共起語ランキング・文脈分析・ブランド認知サマリーへの反映）まで完了しているが、「改善提案」（`improvement_suggestions.py`）への反映は方針整理段階であり、実装はまだ。新規[14_common_crawl_improvement_policy.md](./14_common_crawl_improvement_policy.md)に、AI学習内容そのものとは断定せずWeb情報環境の補助ソースとして扱う表現方針、使ってよい観点・避けるべき断定表現、`meta.commonCrawlProvider.status`に応じた仮文言案（依頼者確認前）、実装ステップ、依頼者確認が必要な表現（「AI学習データ推定」表現の可否等）を整理した。**今回はdocsのみで、コード変更（`backend/`/`app/`とも）は一切行っていない**。詳細は[14_common_crawl_improvement_policy.md](./14_common_crawl_improvement_policy.md)参照
- Common Crawl statusを改善提案へ軽く反映（`feature/common-crawl-improvement-suggestion`、2026-07-28）。直前の方針docsの最小実装案を実装した。`meta.commonCrawlProvider.status`に応じて改善提案を最大1件追加——`"off"`（未使用）では追加しない、`"real"`では「Common Crawl補完で確認できる文脈の一貫性を高める」（`priority: medium`）、`"unavailable"`では「クロールされやすい重要ページを整備する」（`priority: low`）。断定表現（「必ず学習」「必ず改善」「ランキング要因」等）は避け、`reason`全文・HTML/WARC本文はいずれも提案本文に含めない。新規`backend/services/improvement_suggestions.py`の`_common_crawl_suggestion()`のみで実装し、`backend/main.py`は既存の`common_crawl_provider`をそのまま新規引数として渡すだけ。既存の`ImprovementSuggestion`型（`title`/`description`/`priority`）は無変更（category相当のフィールドは元々存在しないため追加せず）。テストは`backend/tests/test_improvement_suggestions.py`に8件・`backend/tests/test_main.py`に7件追加。**Common Crawl取得service層・UI・DataForSEO/ChatGPT関連コードはいずれも変更していない**。提案文言は依頼者確認前の仮のもの。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「16. Common Crawl statusの改善提案への軽い反映」参照
- Common Crawl status表示を分かりやすく整理（`style/common-crawl-status-display`、2026-07-28）。「共起語ランキング」カード付近のCommon Crawl状態表示（`app/lib/meta-label.ts`の`getCommonCrawlProviderDisplay()`）を非エンジニア向けに整理した。`status="off"`は「Common Crawl補完: オフ」→「Common Crawl補完: 未使用」。`status="unavailable"`は、backendの開発者向け`reason`文字列をそのまま表示していた従来の表示をやめ、「Common Crawl補完: 補完データ未取得」＋新規`classifyCommonCrawlUnavailableReason()`が分類した短い理由（「補完対象ページが見つかりませんでした」等、5パターン）を別行で表示する。`status="real"`は基本維持しつつ、詳細行のラベルを「Index:」から「クロールIndex:」に変更。`CooccurrenceRankingSection.tsx`・分析ソース内訳表示は無変更。テストは`meta-label.test.ts`に8件追加。**backend・改善提案ロジック・DataForSEO/ChatGPT関連コードはいずれも変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「17. Common Crawl status表示の整理」参照
- Common Crawl status表示の英語reasonを再確認・強化（`fix/common-crawl-status-japanese-reasons`、2026-07-28）。前タスクのマージ後、実環境で旧表示形式（英語`reason`をそのまま表示する形）が見えたとの報告を受け再調査した。backendが返しうる`reason`文字列11種類すべてを検証した結果、現行コードは既に正しく日本語分類しておりコード上の欠陥は見つからなかった（古いデプロイ・キャッシュを見ていた可能性）。安全のため、domain未確定系の分類パターンをより広い部分一致（`hostname`単体等）に強化し、`app/lib/meta-label.test.ts`に8件のテスト（報告文字列・全reason文字列一括・単語単体・技術的文字列混入ケース）を追加した。**backend・改善提案ロジック・DataForSEO/ChatGPT関連コードはいずれも変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「18. Common Crawl status表示の英語reason再確認・強化」参照
- Common Crawl取得ページ一覧をメタ情報として表示（`feature/common-crawl-analyzed-urls-display`、2026-07-28）。Common Crawlで実際に取得し分析入力に使ったページURLの一覧を、依頼者確認・デバッグ・今後の上限拡張に備えて表示できるようにした。新規`backend/models.py`の`CommonCrawlProviderInfo.analyzedUrls: list[str] = []`に、`status="real"`の場合のみ実際にDocument化できた各URLを（取得失敗した候補は除外、重複URLも除外して）格納し、新規`app/lib/meta-label.ts`の`getCommonCrawlAnalyzedPagesDisplay()`が「取得ページ」（仮ラベル）として「共起語ランキング」カードへ表示する（`target="_blank"`/`rel="noreferrer"`付きリンク）。**3件上限は今回も維持し、全件取得・非同期ジョブ化・DB保存は行っていない**（Render環境のメモリ・timeoutリスク、WARC取得/HTML抽出の重さ、分析結果の説明性を優先する理由をdocsに明記）。テストはbackend 7件・frontend 8件を追加。**Common Crawl取得service層・改善提案ロジック・DataForSEO/ChatGPT関連コードはいずれも変更していない**。あわせて`backend/README.md`の旧表示記述も現在の表示仕様に修正した。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「19. Common Crawl取得ページ一覧の表示」参照
- Common Crawl Index API失敗時の診断ログを強化（`chore/common-crawl-index-diagnostics`、2026-07-29）。Render上でCommon Crawl補完が即時失敗する事象（`COMMON_CRAWL_INDEX`固定・`COMMON_CRAWL_TIMEOUT_SECONDS`変更でも改善せず、待たされずに失敗）が報告されたが、当時のログ「`...request failed (network/timeout error)`」だけでは例外種別・接続先URLが分からなかった。`backend/services/common_crawl_index.py`のrequest開始時ログ（index/domain/url pattern/timeout実効値/request URL）、失敗時ログの`error_type`/例外メッセージ追加、non-200時のstatus code/200文字までのbody preview追加により、Renderログだけで原因を切り分けられるようにした。**取得ロジック・retry・fallback index・取得件数・画面表示用reasonはいずれも変更していない**。テストは`test_common_crawl_index.py`に9件追加。**DataForSEO/ChatGPT関連コードは変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「20. Common Crawl Index API失敗時の診断ログ強化」参照
- Common Crawl Index APIにretryを追加（`fix/common-crawl-index-retry`、2026-07-29）。前タスクの診断ログにより、Render上の実際の失敗が`httpx.RemoteProtocolError`（「Server disconnected without sending a response.」——タイムアウト経過を待たず即座に発生し、`COMMON_CRAWL_TIMEOUT_SECONDS`を伸ばしても効果がない切断系エラー）であることが判明した。`search_common_crawl_domain()`/`_fetch_latest_index()`双方に最大3回（初回+retry2回、0.5秒→1.0秒待機）の軽いretryを追加した。retry対象は`httpx.TransportError`（RemoteProtocolError/ConnectError/ConnectTimeout/ReadTimeout等）と非200のうち502/503/504のみ（400/404はretryしない）。各attemptに`attempt=N/3`ログ、retry時に`retrying`ログ、全滅時に`exhausted retries`ログ、リトライ成功時に`succeeded`ログを追加。**3回とも失敗した場合の画面表示用reasonは従来と完全に同じで、取得件数・fallback indexは変更していない**。`COMMON_CRAWL_TIMEOUT_SECONDS`の許可範囲（3〜30秒、範囲外は10秒にフォールバック）をdocsに明記。テストは`test_common_crawl_index.py`に14件追加、既存の診断ログ関連テストもretry対象例外の実行回数変化に合わせて更新（`time.sleep`はすべてmonkeypatchで潰しており実時間の待機なし）。**DataForSEO/ChatGPT関連コードは変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「21. Common Crawl Index API retry追加」参照
- Common Crawl Index APIにquery形式のfallbackを追加（`fix/common-crawl-index-query-fallback`、2026-07-29）。retry追加後もRenderで標準query（filter付き）が3回とも`RemoteProtocolError`で失敗し続ける事象が確認され、retry自体は正しく動作しているが同じquery形式を繰り返すだけでは復旧しないケースがあると判明した。`search_common_crawl_domain()`に段階的なquery variant fallback（標準query→filterなしquery→`www.`付きfilterなしquery、各variantに既存の最大3回retryを適用）を追加した。fallbackするのは`httpx.TransportError`または502/503/504がretryしても解消しない場合のみで、400/404・0件（query自体は成功）はfallbackしない。query_variantログ・fallbackログ・全滅時のall query variants failedログを追加。**取得成功時の候補抽出ロジック・画面表示用reason・取得件数・`common_crawl_warc.py`/`common_crawl_document_provider.py`はいずれも変更していない**。`COMMON_CRAWL_TIMEOUT_SECONDS`はRenderでは30推奨（許可範囲3〜30秒は現行維持）である旨をdocsに明記。テストは`test_common_crawl_index.py`に12件追加、既存の診断ログ・retry関連テストも最大試行回数の変化（3→9回）に合わせて更新（`time.sleep`はすべてmonkeypatchで潰しており実時間の待機なし）。**DataForSEO/ChatGPT関連コードは変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「22. Common Crawl Index API query形式fallback追加」参照
- Common Crawl Index API request headersを明示（`fix/common-crawl-index-request-headers`、2026-07-29）。query fallback追加後もRenderで**全query variant**が3回ずつ`RemoteProtocolError`で失敗し続ける事象が確認され、query形式だけの問題ではなさそうと判断した。`search_common_crawl_domain()`/`_fetch_latest_index()`のIndex APIリクエストに明示的な`User-Agent`（`CommonCrawlSettings.user_agent`、従来WARC取得のみで使用）・`Accept`（`application/json, text/plain;q=0.9, */*;q=0.8`）・`Connection: close`（keep-alive/コネクション再利用まわりの問題を疑う）を追加した。headersはretry中・query fallback後も同一のものを使い回す。request開始ログに`user_agent=%s accept=%s connection=%s`を追加（raw headers dictの丸ごと出力はしない）。**取得成功時の候補抽出ロジック・画面表示用reason・取得件数・`trust_env`はいずれも変更していない**。`trust_env=False`や別HTTP client方式は、今回のheaders追加でも改善しない場合の次の検討候補としてdocsに記載するのみに留めた。テストは`test_common_crawl_index.py`に13件追加、既存のheaders完全一致アサーション1件も新規headersを含む形に更新（`time.sleep`はすべてmonkeypatchで潰しており実時間の待機なし）。**DataForSEO/ChatGPT関連コードは変更していない**。詳細は[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)「23. Common Crawl Index API request headers明示」参照
- 依頼者確認用のVercel/Render公開（[09_deployment.md](./09_deployment.md)参照）
- ステージング環境の最低限保護: 簡易パスコードガード（[proxy.ts](../proxy.ts)、`STAGING_ACCESS_CODE`未設定時はローカル開発に影響なし）と`noindex`設定（[app/layout.tsx](../app/layout.tsx)の`metadata.robots`＋`X-Robots-Tag`ヘッダー）

## 一部実データの機能

- **共起語ランキング（`cooccurrenceRanking`）**が実計算。ブランド名前後20文字ウィンドウ＋トークナイザーという単純な方式（[services/cooccurrence.py](../backend/services/cooccurrence.py)、[03_api_design.md](./03_api_design.md)参照）。トークナイザーは`TOKENIZER_MODE`環境変数で切り替え可能で、**デフォルトは辞書不要の軽量`simple`モード**（正規表現による英数字/日本語文字種境界での簡易分割、品詞フィルタなし）。Janomeによる形態素解析（品詞フィルタつき、より高精度）は`TOKENIZER_MODE=janome`を明示した場合のみのoptional扱い（2026-07-16変更。理由: Render無料枠512MBで`/analyze`実行時にJanomeの辞書読み込みが原因の502/timeoutが発生していたため、確認用環境では精度よりも安定動作を優先。詳細は[11_architecture_v1.md](./11_architecture_v1.md)のAnalyzer節）。精度向上は未着手（[05_tasks.md](./05_tasks.md) Phase 4.2）。
- `simple`モードの明らかなノイズ削減を実施済み（2026-07-16）。①ブランド前後20文字ウィンドウがASCII単語の途中で切れる場合に単語境界まで拡張（例:「second」の途中で切れて「nd」が出る問題を解消）、②英語の一般的な機能語（on/to/in/of/the等）をstopwordsに追加、③ASCII側トークンのみ最小3文字に強化（`AI`のような2文字語は今回は許容して除外）。日本語側の最小2文字ルールは変更なし。
- **文脈分析（`contextAnalysis`）**も実計算（`backend/services/context_analysis.py`、2026-07-16、"context-analysis-lite"）。ただしキーワード一致による軽量な分類にとどまり、AIによる意味理解・要約ではない（詳細は上記「実装済み機能」・[11_architecture_v1.md](./11_architecture_v1.md)参照）。本格的な文脈理解・Embedding・Knowledge Graphは引き続き未実装。
- **ブランド認知サマリー（`summary`）**も実計算（`backend/services/brand_summary.py`、2026-07-16、"brand-summary-lite"）。ただしAI要約ではなくルールベース・テンプレート生成であり、`visibilityScore`はMVP用の簡易推定値、`topPlatforms`は実測していないAIプラットフォーム名ではなく実際の`Document.sourceType`ラベル（詳細は上記「実装済み機能」・[11_architecture_v1.md](./11_architecture_v1.md)参照）。
- **改善提案（`improvements`）**も実計算（`backend/services/improvement_suggestions.py`、2026-07-16、"improvement-suggestions-lite"）。ただしAI/LLM/DataForSEOによる提案生成ではなく、既存の分析結果に対する説明可能なルールベーストリアージであり、最終的なSEO/LLMO施策の採否判断には人間の確認が必要（詳細は上記「実装済み機能」・[11_architecture_v1.md](./11_architecture_v1.md)参照）。

## まだダミー（固定データ）の機能

- AI Overview比較（`aiOverviewComparison`）— provider切り替え基盤（`backend/services/ai_overview_provider.py`、2026-07-17）は導入済みだが、デフォルトの`mock`モードでは引き続き固定データを返す。`dataforseo`モードはDataForSEO **Sandbox**への接続を実装済み（成功時は`"real"`）。**Live本番APIへの接続も実装済みだが、5つの手動確認用ゲートがすべて揃った場合のみの1回限りの利用に限定される**（常時運用ではない。`AI_OVERVIEW_PROVIDER_MODE=off`にすれば`"unavailable"`として無効化することも可能。詳細は上記「実装済み機能」・[11_architecture_v1.md](./11_architecture_v1.md)参照）。同セクションへの追加要素であるChatGPT相当モデルの1問観測（`chatgpt_provider.py`、2026-07-23）も、`CHATGPT_PROVIDER_MODE`のデフォルトが`off`のため通常はカードを追加しない（明示的な条件を満たした場合のみ`"real"`のChatGPTカードが1件加わる）。

`meta.sections.*` が `"mock"` になっているセクションがこれに該当する。

## 未接続の外部サービス・機能

- Common Crawl（Phase 3、settings + Index API検索クライアント + WARC fetch/HTML extraction service + `Document[]`変換service + `/analyze`統合（最大3件・最大5候補試行） + 検証用UI selector + 改善提案への軽い反映（`status`に応じた1件） + status表示の整理（off/unavailable時の非技術的な文言化） + 取得ページ一覧の表示（`analyzedUrls`） + Index API失敗時の診断ログ強化 + Index API retry追加 + Index API query形式fallback追加 + Index API request headers明示まで実装済み・2026-07-29。診断ログでRender上の実際の失敗原因（`httpx.RemoteProtocolError`）が判明し、retry・query fallbackを追加後も**全query variant**で失敗し続けたため、明示的なUser-Agent/Accept/Connection: closeを追加済み（実環境での再検証は未実施。表示名・説明文・改善提案文言・「取得ページ」ラベルの依頼者確認は未着手）。デフォルトoff（`COMMON_CRAWL_ENABLED=false`・`NEXT_PUBLIC_ENABLE_COMMON_CRAWL_MODE_SELECTOR=false`）で、通常の画面・挙動には未反映——検証時のみ両方をtrueにした場合に動作する。詳細は上記「実装済み機能」・[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)参照）
- DataForSEO **Live** APIの常時運用・自動スケジュール実行（Phase 3、未着手。Sandboxへの基本接続、および手動確認用ゲート付きのLive接続はPhase 4側で実装済み、[11_architecture_v1.md](./11_architecture_v1.md)参照）
- PostgreSQL（Phase 5、未着手。分析結果は保存されず、画面をリロードすると消える）
- 認証・ユーザー管理（Phase 6、未着手）
- 課金・プラン管理（非スコープ）
- ChatGPT相当モデル観測の常時運用・複数質問対応・Claude/Gemini等の他モデルへの拡張（1問限りの検証用実装（`chatgpt_provider.py`）はデフォルトoffで導入済み、[11_architecture_v1.md](./11_architecture_v1.md)参照）
- CD・自動レビュー・自動マージ（CIの最小構成（lint/test/build・pytest）は追加済みだが、CD（Vercel/Renderへの自動デプロイ）・AIレビューの自動化・人間承認後の自動マージはいずれも未導入。詳細は[10_ai_development_workflow.md](./10_ai_development_workflow.md)参照）

## 公開URL（依頼者確認用ステージング環境）

| サービス | URL |
| --- | --- |
| Vercel（Next.js） | <https://ai-visibility-platform-eight.vercel.app/> |
| Render（FastAPI、`/health`） | <https://llmo-analysis-api.onrender.com/health> |

この環境は**依頼者確認用ステージング環境**（*staging-demo environment*）であり、正式な本番環境・一般公開サービスではない。用途は進捗報告・機能確認・MVPレビュー・UI/API連携確認に限る。将来的には独自ドメイン環境へ移行予定（詳細は[09_deployment.md](./09_deployment.md)の「環境の位置づけ」参照）。

## 確認用環境の制約

- **本番運用ではない**。数ヶ月程度の依頼者確認用として運用する想定（[09_deployment.md](./09_deployment.md)参照）。
- **簡易パスコードガードあり（`STAGING_ACCESS_CODE`）だが、正式な認証ではない**。ブルートフォース対策・アカウント管理はない。誤アクセス防止・検索露出低減が目的（[09_deployment.md](./09_deployment.md)の「簡易パスコードガード」参照）。レート制限・利用量監視は引き続きない。
- 検索エンジンには`noindex`設定済み（[09_deployment.md](./09_deployment.md)の「noindexの設定」参照）。
- 共有先は必要な相手に限定し、機密情報・個人情報・本番データは入力しないよう依頼者にも伝える。
- Render無料プランのため**コールドスタートがある**（スリープからの復帰に約20〜25秒。この間、Python APIが間に合わずダミーデータにフォールバックすることがある。障害ではなく既知の仕様）。詳細は [09_deployment.md](./09_deployment.md) の「コールドスタートに関する注意」参照。
- 分析結果は永続化されない（PostgreSQL未接続）。
- URL共有時は[09_deployment.md](./09_deployment.md)の「依頼者への共有文テンプレート」を使う。

## 既知の課題

- 簡易パスコードガードは導入したが、あくまで誤アクセス防止用。確認終了後、公開を止めるか正式な認証を追加するかの判断はまだ済んでいない（[05_tasks.md](./05_tasks.md) Phase 4.5）。
- 共起語抽出は簡易な実装であり、ウィンドウ重複によるカウント過多・ウィンドウ外の関連語の取りこぼしがある（[07_decisions.md](./07_decisions.md)に既知の制約として記録済み）。加えて、デフォルトの軽量`simple`トークナイザーは品詞情報を持たないため、Janomeより単語境界の精度が低い（例: 連続する漢字の複合語を分割できない）。`on`/`to`/`nd`のような明らかなノイズは削減済み（2026-07-16、上記「一部実データの機能」参照）、「には」/「くことが」/「しくなる」のような助詞・活用語尾の断片も第二段フィルタ`is_low_value_cooccurrence_term()`で削減済み（2026-07-28、`fix/cooccurrence-noise-filter`、上記「実装済み機能」参照）だが、いずれも網羅的なstopwordsリストではないため、未知のノイズ語が残る可能性がある。
- 文脈分析（context-analysis-lite）はキーワードの単純な出現回数勝負でカテゴリを決めるため、複数カテゴリのキーワードが同数ヒットした場合は`CATEGORY_KEYWORDS`の宣言順（`pricing`→`feature`→...）で先勝ちになる。例えば「対応」（`feature`）と「サポート」（`support`）が同数ヒットすると`feature`が選ばれる。意味的な優先度ではなく実装上の仕様であり、既知の制約として残す（[11_architecture_v1.md](./11_architecture_v1.md)参照）。
- ブランド認知サマリー（brand-summary-lite）の`visibilityScore`は言及数・Document件数等を加算するだけのMVP用の簡易推定値であり、実際の生成AIにおける認知度を測定したものではない。`sentimentBreakdown`も`contextAnalysis`のカテゴリ傾向（`feature`→positive等）から均等重みで振り分けるだけで、文章そのものの感情分析ではない。いずれも今後高度化する余地がある既知の制約（[11_architecture_v1.md](./11_architecture_v1.md)参照）。
- 改善提案（improvement-suggestions-lite）はAI/LLMによる提案生成ではなく、`contextAnalysis`のカテゴリ有無等に対する説明可能なルールベースのトリアージにとどまる。カテゴリの有無は二値判定（各カテゴリ最大1件しか`contextAnalysis`に現れないため、「少ない」という段階的な判定はできない）であり、DataForSEO等の実測データとの統合もまだない。最終的なSEO/LLMO施策の採否判断には人間の確認が必要（[11_architecture_v1.md](./11_architecture_v1.md)参照）。
- AI Overview比較（`ai_overview_provider.py`）は`mock`/`off`/`dataforseo`のprovider切り替え基盤・認証情報/実行安全ルールの設計（`dataforseo_settings.py`）・DataForSEO **Sandbox**への実接続（`dataforseo_client.py`）に加え、**5つの手動確認用ゲートが揃った場合に限るLive本番API接続**まで完了している。`dataforseo`モード＋`DATAFORSEO_API_ENV=live`でも、いずれかのゲート（`DATAFORSEO_LIVE_API_ENABLED`/`DATAFORSEO_LIVE_CONFIRM_TEXT`/`DATAFORSEO_REQUEST_LIMIT_PER_ANALYZE`/認証情報）が欠けていれば外部APIは呼ばれず`"unavailable"`になる。常時のLive運用・自動スケジュール実行は今回も未着手。またSandboxのレスポンスは実際の本番SERPデータではなくテスト用モックデータである点（Live接続時のレスポンスは実際の本番SERPデータであり費用が発生し得る点）、Google AI OverviewとGoogle AI Modeが同一のレスポンス構造で表現されるかはSandboxでのみ確認済みでLive本番ホストに対する検証は未実施である点にも注意（[11_architecture_v1.md](./11_architecture_v1.md)参照）。
- `ownDomainReferenced`（2026-07-23追加）はリクエストの`urls`のドメインと`references`のドメインの単純な文字列一致にとどまり、参照元ページの実際の内容確認・スコアリング・競合ドメインの分類は行わない。`references`自体もDataForSEOが返した`title`/`domain`/`url`等のメタ情報のみで、参照先ページを実際にフェッチして内容を検証することはしない。`references[].category`/`referenceSummary`（同日追加）も同様に、小さなハードコードdomainリストとの照合によるルールベース簡易分類にとどまる——AIによる分類や高精度なニュース/メディア判定ではなく、`"media"`カテゴリは値として予約されているのみで現時点では何も分類されない。
- ChatGPT相当モデルの1問観測（`chatgpt_provider.py`）はOpenAI APIのモデルへの1回の質問と回答にとどまり、ChatGPTアプリ画面そのものの内部認識を再現するものではない。Web検索・参照元付き回答は使わず、`rank`/`references`/`referenceSummary`/`ownDomainReferenced`も持たない（すべて`None`固定）。1 `/analyze`あたり最大1回に固定しているため、複数回の質問による傾向把握はできない（[11_architecture_v1.md](./11_architecture_v1.md)参照）。`CHATGPT_TEMPERATURE`（デフォルト`0.2`、2026-07-28追加）により回答のばらつきは抑えているが、`temperature`は完全に`0`ではないため毎回一言一句同じ回答になることは保証されない（デモ・検証時の安定性向上が目的であり、決定論的な再現ではない）。また`gpt-5`系モデル（`gpt-5`/`gpt-5-mini`等）は`temperature`パラメータ自体を受け付けないため、`chatgpt_client.py`がモデル名で判定してリクエストボディから自動的に省略する（2026-07-28追加、`should_send_temperature()`）——`gpt-4.1`系/`gpt-4o`系では引き続き`temperature`が送信される。
- `documents`（文章を直接渡す入力）にはまだ画面からの入力UIがない（`urls`のみUIがある）。
- URL取得はrobots.txt確認・レート制限・DNS rebinding対策が未実装（運用者の責任として文書化のみ、[backend/README.md](../backend/README.md)参照）。
- ~~GitHub ActionsでNode.js 20 deprecation warning~~ → 調査済み・対応済み（2026-07-15）。GitHubがActions runner上のNode.js 20ランタイムを段階的に廃止しており（2026-06-16よりNode24がデフォルト、2026-09-16に完全廃止予定）、`actions/checkout`・`actions/setup-node`・`actions/setup-python`がNode.js 20ランタイムで動いていることに起因する警告だった。`.github/workflows/ci.yml`の各actionを、Node24対応が確認できるバージョン（`actions/checkout@v5`、`actions/setup-node@v5`、`actions/setup-python@v6`）へ更新して解消した。**アプリのビルド/テストに使う`node-version: "20"`（Next.js 16系の要件）とは無関係**であり、これは変更していない。

## 次にやるべき候補

優先度の目安。詳細・粒度は [05_tasks.md](./05_tasks.md) を参照。

1. 確認用公開環境の今後を決める（止める／簡易パスコードのままにする／正式な認証を足す）
2. ~~Chunkerの出力をAnalyzerに繋ぐ~~ → 文脈分析（context-analysis-lite）が最初にChunker出力を消費するAnalyzerロジックとして完了（2026-07-16）。共起解析自体は引き続き`Document.text`を直接読み、Chunker非経由（[11_architecture_v1.md](./11_architecture_v1.md)「10. 次フェーズ候補」参照）
3. CI/CDのCD側（Vercel/Renderへの自動反映）の検討
4. 共起語抽出の精度向上、または文脈分析・ブランド認知サマリー・改善提案のルールベースからの高度化（意味的理解・要約等）
5. ~~AI Overview比較のDataForSEO Live API接続~~ → 5つの手動確認用ゲート（`DATAFORSEO_API_ENV=live`・`DATAFORSEO_LIVE_API_ENABLED=true`・`DATAFORSEO_LIVE_CONFIRM_TEXT`完全一致・`DATAFORSEO_REQUEST_LIMIT_PER_ANALYZE=1`・認証情報設定済み）がすべて揃った場合のみの手動1回確認として完了（2026-07-23、`feature/dataforseo-live-manual-gate`）。次はこれをRenderで実際に確認する運用（本物のAPIキー投入・費用発生の判断）、および常時運用化するかどうかの検討
6. ~~Phase 3（Common Crawl調査開始）~~ → 最小MVPの設計docs（[13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)）→ settings + Index API検索クライアント（2026-07-28、`feature/common-crawl-index-client`）→ WARC fetch/HTML extraction service（2026-07-28、`feature/common-crawl-warc-fetch`）→ `Document[]`変換service（2026-07-28、`feature/common-crawl-document-provider`）→ `/analyze`統合（2026-07-28、`feature/common-crawl-analyze-integration`）→ 検証用UI selector（2026-07-28、`feature/common-crawl-ui-selector`）→ 複数件取得への拡張・最大1件から最大3件へ（2026-07-28、`feature/common-crawl-multiple-documents`）→ 改善提案反映方針docs（2026-07-28、`docs/common-crawl-improvement-policy`）→ Common Crawl statusの改善提案への軽い反映（2026-07-28、`feature/common-crawl-improvement-suggestion`）→ 依頼者確認用メモの集約（2026-07-28、`docs/requester-review-items-common-crawl`）→ Common Crawl status表示の整理（2026-07-28、`style/common-crawl-status-display`）→ Common Crawl status表示の英語reason再確認・強化（2026-07-28、`fix/common-crawl-status-japanese-reasons`）→ 取得ページ一覧の表示（2026-07-28、`feature/common-crawl-analyzed-urls-display`）→ Index API失敗時の診断ログ強化（2026-07-29、`chore/common-crawl-index-diagnostics`）→ Index API retry追加（2026-07-29、`fix/common-crawl-index-retry`）→ Index API query形式fallback追加（2026-07-29、`fix/common-crawl-index-query-fallback`）→ Index API request headers明示（2026-07-29、`fix/common-crawl-index-request-headers`）まで実装完了。次はRender環境での再検証 → trust_env=False/別HTTP client方式の検討 → 0件時のquery拡張検討 → 表示名・説明文の依頼者確認（[15_requester_review_items.md](./15_requester_review_items.md)参照）、の順で段階的に実装着手。DataForSEO Live APIの常時運用化・複数キーワード対応・費用管理の検討は引き続き未着手
7. ChatGPT相当モデルの1問観測（`feature/chatgpt-observation-provider`、2026-07-23）の常時運用化検討、複数質問対応、Claude/Gemini等の他モデルへの拡張

## 関連ドキュメント

- 要件・スコープ: [01_requirements.md](./01_requirements.md)
- ロードマップ: [02_roadmap.md](./02_roadmap.md)
- タスク一覧（詳細）: [05_tasks.md](./05_tasks.md)
- 設計判断ログ: [07_decisions.md](./07_decisions.md)
- 解析エンジンのv1.0アーキテクチャ（Document Pipeline等）: [11_architecture_v1.md](./11_architecture_v1.md)
- 公開手順: [09_deployment.md](./09_deployment.md)
- デモ提出用チェックリスト（推奨env・入力例・見せる順番）: [12_demo_readiness.md](./12_demo_readiness.md)
- Common Crawl最小連携の設計（MVP）: [13_common_crawl_mvp_design.md](./13_common_crawl_mvp_design.md)
- Common Crawl関連 依頼者確認用メモ（表示名・説明文・改善提案文言）: [15_requester_review_items.md](./15_requester_review_items.md)
- AI協調開発フロー（Claude Codeの中断・再開ルールを含む）: [10_ai_development_workflow.md](./10_ai_development_workflow.md)
- タスク依頼・レビュー・中断/再開時の報告フォーマット: [task_template.md](./task_template.md) / [review_template.md](./review_template.md)
